{
  "name": "martin-flying-machines",
  "version": "2.0.0",
  "description": "Martin's Flying Machines - Micro RC Aviation Archive",
  "main": "src/index.js",
  "scripts": {
    "prebuild": "mkdir -p public && cp -r css planes Pictures Images Videos Plans index.html public/",
    "dev": "npm run prebuild && wrangler dev",
    "deploy": "npm run prebuild && wrangler deploy",
    "tail": "wrangler tail"
  },
  "keywords": [
    "cloudflare",
    "workers",
    "static-site",
    "rc-aviation"
  ],
  "author": "Antigravity AI",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
